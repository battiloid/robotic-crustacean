spring:
  application:
    name: gateway
  profiles:
    active: native
  cloud:
    config:
      name: gateway
      discovery:
        service-id: config-server
        enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-peer1:8761/eureka
